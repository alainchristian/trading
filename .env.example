# Database (isolated PostgreSQL instance dedicated to this project, port 5433 —
# NOT the existing forex-system instance on 5432)
DB_HOST=localhost
DB_PORT=5433
DB_NAME=trading_platform
DB_USER=trading_app
DB_PASSWORD=changeme

# Bridge service
BRIDGE_HOST=127.0.0.1
BRIDGE_PORT=8000
LOG_LEVEL=INFO
ENVIRONMENT=development
